note: 'Classification is per call site. "Absence means zero" has to be true of the DOMAIN, not merely
  convenient for the dtype: a sector carrying no weight in a leg really is zero weight, whereas a
  day with no observed return is not a flat day. Keyed on file plus code fragment rather than line
  number: the first version keyed on line numbers and broke the moment an unrelated edit shifted
  one.'
recorded_on: 2026-09-02
sites:
- where: llm/extract.py
  what: article counts per category per date
  verdict: legitimate
  why: 'A category with no articles on a date genuinely has zero articles. The share computed on
    the next line already guards the degenerate case by replacing a zero denominator with NA rather
    than dividing.

    '
  code: ).fillna(0)
- where: features/composition.py
  what: winner-minus-loser sector weight tilt
  verdict: legitimate
  why: A sector absent from a leg carries zero weight in that leg. That is a true zero.
  code: return w.reindex(idx).fillna(0) - l.reindex(idx).fillna(0)
- where: features/composition.py
  what: dense sector-by-date tilt panel
  verdict: legitimate
  why: Same as above, densified across dates. A sector missing on a date holds no weight.
  code: return pd.DataFrame(rows).T.fillna(0).sort_index()
- where: factor/legs.py
  what: momentum score, compounding formation-window returns
  verdict: guarded
  why: 'Deliberate and documented: holdings prices are unadjusted, so a reverse split is a step change
    and compounding masked returns treats the split day as flat, which is approximately the true
    economic return. Bounded by a coverage gate requiring 80 percent of the formation window to be
    present. That gate still admits up to 20 percent zero-filled days inside a surviving name''s
    score, which is the residual exposure and is disclosed rather than fixed.

    '
  code: score = (1.0 + formation.fillna(0.0)).prod() - 1.0
- where: model/baselines.py
  what: turnover on the first date of a weight series
  verdict: legitimate
  why: The first difference has no predecessor; no prior position means no trade to cost.
  code: turnover = w.diff().abs().fillna(0.0)
- where: data/gdelt.py
  what: article counts per mechanism per date
  verdict: legitimate
  why: 'Dates come from the data itself, so a date with no articles never enters the index. Within
    a date that does exist, a mechanism with no articles genuinely has zero.

    '
  code: d.groupby(["date", "mechanism"]).size().unstack("mechanism").fillna(0).sort_index()
- where: data/ishares.py
  what: price filter dropping non-positive prices
  verdict: legitimate
  why: 'A filter, not a feature. A NaN price becomes zero and is then excluded, so the fill drops
    the row rather than inventing an observation for it.

    '
  code: out = out.loc[out["price"].fillna(0) > 0]
- where: validation/power.py
  what: NaN correlations in a submatrix before an eigenvalue
  verdict: conservative
  why: 'Filling an unknown correlation with zero asserts independence, which lowers the leading eigenvalue
    and therefore UNDERSTATES concentration. It biases against the claim being tested, but it is
    still an assumption rather than a measurement and is recorded as one.

    '
  code: sub = np.nan_to_num(sub, nan=0.0)
