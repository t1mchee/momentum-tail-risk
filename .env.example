# The live model seat reads this. Every deterministic panel and every replay command
# works without it; only scripts/gate2_extract.py, scripts/gate2_debate.py and the
# dashboard's /api/extract endpoint need a key.
ANTHROPIC_API_KEY=sk-ant-...
